/**
 * Monochrome design tokens (ink / gray — no rose).
 * Source of truth = docs/mockups/. Fill from the mockups' CSS variables.
 */

export const colors = {
  // TODO: extract from docs/mockups/ (ink scale + grays + surfaces)
  ink: "#111111",
  gray: "#8a8a8a",
  surface: "#ffffff",
} as const;

export const spacing = {
  // TODO
} as const;
