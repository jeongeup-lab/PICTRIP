/** Minimum time the analyzing screen stays visible before navigating to the
 * result, so a fast response does not flash the spinner. */
export const MIN_VISIBLE_MS = 600;

/** One sweep of the indeterminate progress bar (ms). */
export const LOADING_SWEEP_MS = 1300;

/** One jump between candidate photo cards while the pick animation loops. */
export const HOP_MS = 620;

/** The closing jump that lands on the picked photo once the search succeeds. */
export const FINALE_HOP_MS = 520;

/** How long the finale pose holds before the analyzing screen signals done. */
export const FINALE_HOLD_MS = 420;
