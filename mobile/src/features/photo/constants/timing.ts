/** Minimum time the analyzing screen stays visible before navigating to the
 * result, so a fast response does not flash the spinner. */
export const MIN_VISIBLE_MS = 600;

/** One sweep of the indeterminate progress bar (ms). */
export const LOADING_SWEEP_MS = 1300;
