# S13 Plan C — 모바일 (탭바 · 홈 타일+피드 · 스토리 뷰어 · 탐색 · 사진 제거)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** UI SSOT `docs/mockups/redesign-2026-07-cc/app-prototype.html`대로 홈·탐색을
구현하고 사진 탭·사진 검색을 제거한다. C1~C5는 라우트 미연결 additive(머지 안전),
C6이 원자적 플립 PR.

**Architecture:** `features/channels`(타일+뷰어) · `features/feed` 신규 파일(게시물
캐러셀; 구 파일과 공존 후 C6에서 구 파일 삭제) · `features/explore`. 캐러셀은
FlatList pagingEnabled(PhotoViewer 패턴), 시트는 AuthPromptSheet 패턴, 그라데이션은
react-native-svg LinearGradient(SpotHero 패턴). 신규 네이티브 모듈 0.

**Tech Stack:** Expo Router(typed) · TanStack Query(**useInfiniteQuery 첫 도입**) ·
zustand · react-native-svg · expo-secure-store(본 채널 상태).

**Global Constraints:** `2026-07-12-s13-00-overview.md` §Global Constraints·§공유 계약.
브랜치 `feat/s13-c-mobile`. 게이트:
`cd mobile && npm run lint && npm run typecheck && npm run format:check && npm test`.
체크아웃 직후 node_modules 자기참조 심링크 문제가 보이면: skip-worktree + rm + `npm install`.

**디자인 토큰 매핑(프로토타입 → theme.ts):** `--accent`→`colors.accent`,
`--scrim-strong`→`colors.scrimStrong`, `--glass`→`colors.glassFill`,
`--glass-border`→`colors.glassBorder`, `--on-image`→`colors.onImage`,
`--on-dim`→`colors.onDim`, `--skeleton`→`colors.skeleton`. 수치(86×110, radius 14/16,
카드 높이 520, 라벨 11.5/800, 이름 24/800 등)는 프로토타입 CSS가 SSOT.

---

### Task C1: features/channels — API·쿼리·본(seen) 상태

**Files:**
- Create: `mobile/src/features/channels/api.ts`, `features/channels/queries.ts`,
  `features/channels/lib/seen-store.ts`
- Modify: `mobile/src/lib/storage.ts` (day-stamped JSON 헬퍼)
- Test: `features/channels/__tests__/api.test.ts`, `lib/__tests__/seen-store.test.ts`

**Interfaces:**
- Consumes: `api`(`@/lib/api-client`, JSend 언랩 완료 상태로 resolve), storage.ts의
  `getItemAsync/setItemAsync` 패턴.
- Produces:
  ```ts
  type ChannelKey = "around" | "hot" | "hidden" | "festa" | "pets" | "snap";
  type ChannelMeta = { key: ChannelKey; label: string; thumbnailUrl: string | null; available: boolean };
  type ChannelCard = { contentId: string | null; title: string; regionLabel: string;
    imageUrl: string | null; dist: number | null; rank: number | null;
    dday: string | null; line: string | null; tag: string | null; saveable: boolean };
  getChannels(): Promise<{ channels: ChannelMeta[] }>
  getChannelCards(key: ChannelKey, coords?: { lat: number; lng: number }): Promise<{ key: ChannelKey; label: string; cards: ChannelCard[] }>
  useChannels() · useChannelCards(key, coords?)   // queryKey ["channels"] · ["channel-cards", key]
  useSeenChannels(): { seen: Set<ChannelKey>; markSeen: (k: ChannelKey) => void }
  ```

- [ ] **Step 1: 실패 테스트 작성**

`api.test.ts` — `features/map/__tests__/api.test.ts`의 axios mock 방식:

```ts
import { getChannelCards, getChannels } from "@/features/channels/api";
import { api } from "@/lib/api-client";

jest.mock("@/lib/api-client", () => ({ api: { get: jest.fn() } }));

test("getChannels hits /home/channels", async () => {
  (api.get as jest.Mock).mockResolvedValue({ channels: [] });
  await getChannels();
  expect(api.get).toHaveBeenCalledWith("/home/channels");
});

test("getChannelCards passes coords for around", async () => {
  (api.get as jest.Mock).mockResolvedValue({ key: "around", label: "Around", cards: [] });
  await getChannelCards("around", { lat: 35.1, lng: 129 });
  expect(api.get).toHaveBeenCalledWith("/home/channels/around", {
    params: { lat: 35.1, lng: 129 },
  });
});
```

`seen-store.test.ts` — 날짜 바뀌면 리셋:

```ts
import { loadSeen, saveSeen } from "@/features/channels/lib/seen-store";

jest.mock("@/lib/storage", () => {
  let value: string | null = null;
  return { getSeenChannelsRaw: jest.fn(async () => value),
           setSeenChannelsRaw: jest.fn(async (v: string) => { value = v; }) };
});

test("round-trips today's seen keys", async () => {
  await saveSeen(["hot"], "2026-07-12");
  expect(await loadSeen("2026-07-12")).toEqual(["hot"]);
});

test("resets when the stored day differs", async () => {
  await saveSeen(["hot"], "2026-07-12");
  expect(await loadSeen("2026-07-13")).toEqual([]);
});
```

- [ ] **Step 2: 실패 확인**

```bash
cd mobile && npx jest src/features/channels -v
```
Expected: FAIL (모듈 없음).

- [ ] **Step 3: 구현**

`api.ts`: 위 Produces 시그니처 그대로, `api.get(...) as unknown as T` 캐스팅 관례.
`storage.ts`에 `getSeenChannelsRaw/setSeenChannelsRaw` (SecureStore 키
`"seen_channels"`, 기존 함수 스타일 복제). `lib/seen-store.ts`:

```ts
type SeenPayload = { day: string; keys: string[] };

export async function loadSeen(today: string): Promise<string[]> {
  const raw = await getSeenChannelsRaw();
  if (!raw) return [];
  try {
    const parsed = JSON.parse(raw) as SeenPayload;
    return parsed.day === today ? parsed.keys : [];
  } catch {
    return [];
  }
}

export async function saveSeen(keys: string[], today: string): Promise<void> {
  await setSeenChannelsRaw(JSON.stringify({ day: today, keys }));
}
```
`queries.ts`: `useChannels`(staleTime 5분), `useChannelCards(key, coords)` —
around는 `enabled: key !== "around" || !!coords`. `useSeenChannels`는 zustand 스토어 +
마운트 시 `loadSeen(todayKst())` hydrate, `markSeen`이 저장까지. `todayKst()`는
`new Date().toLocaleDateString("sv-SE", { timeZone: "Asia/Seoul" })`.

- [ ] **Step 4: 통과 + 게이트 + 커밋**

```bash
npx jest src/features/channels -v && npm run lint && npm run typecheck
git add src && git commit -m "feat(mobile): channels API·쿼리·본 상태 저장"
```

---

### Task C2: ChannelTiles — 홈 상단 포토 카드 타일

**Files:**
- Create: `features/channels/components/ChannelTiles.tsx`
- Test: `features/channels/components/__tests__/ChannelTiles.test.tsx`

**Interfaces:**
- Consumes: `useChannels`, `useSeenChannels` (C1), `<Icon name="map-pin">`,
  `RemoteImage`(`@/components/RemoteImage`), theme 토큰.
- Produces: `<ChannelTiles onOpen={(key: ChannelKey) => void} />` — C6 홈이 배치,
  C3 뷰어를 `onOpen`으로 연다.

- [ ] **Step 1: 실패 테스트 작성**

`react-test-renderer` + queries mock (`jest.mock("@/features/channels/queries", ...)`):

```ts
test("renders a tile per available channel with English label", ...);
// channels 5개 mock → findAllByProps({testID:"channel-tile"}).length === 5, "Hot" 텍스트 존재

test("around tile renders pin icon instead of photo", ...);
// thumbnailUrl null + key around → Icon(map-pin) 존재

test("seen tile is dimmed and loses badge", ...);
// seen=["hot"] → hot 타일 opacity 0.55 스타일, new-dot testID 부재

test("tapping a tile calls onOpen with its key", ...);
// props.onPress() → onOpen("hot")
```

- [ ] **Step 2: 실패 확인** — `npx jest ChannelTiles` FAIL.

- [ ] **Step 3: 구현**

수평 `ScrollView`(`showsHorizontalScrollIndicator={false}`, `contentContainerStyle`
padding 16/gap 10). 타일 = `Pressable` 86×110, radius 14, overflow hidden:
사진 채널 → `RemoteImage` 풀블리드 + SVG LinearGradient 하단 스크림(투명→
`rgba(16,14,18,.68)`, SpotHero.tsx:38 패턴) + 하단 중앙 라벨(fontSize 11.5,
fontWeight "800", color `colors.onImage`); around → 배경 `#E7F8EF`→`#D3F2E2` 수직
그라데이션(동일 SVG 기법) + 중앙 `<Icon name="map-pin" size={26}
color={colors.accentText}>` + 라벨 color `colors.accentText`. 뱃지 = 우상단 8pt 원
`colors.accent` + 흰 테두리 1.5. seen → 컨테이너 `opacity: 0.55` + 뱃지 숨김.
`available === false` 채널은 탭 시 무동작 대신 타일 자체 미렌더(메타에서 festa가
빠져 오는 케이스와 동일 취급) — 단 pets/snap `available:false`는 흐림 처리로 표기.

- [ ] **Step 4: 통과 + 커밋**

```bash
npx jest ChannelTiles -v && npm run lint && npm run typecheck
git add src && git commit -m "feat(mobile): 홈 채널 포토 카드 타일"
```

---

### Task C3: 스토리 뷰어 (`/channels` fullScreenModal)

**Files:**
- Create: `features/channels/components/StoryViewer.tsx`, `components/StoryCard.tsx`,
  `mobile/src/app/channels.tsx`
- Modify: `mobile/src/app/_layout.tsx` (Stack.Screen `channels` 등록,
  `presentation: "fullScreenModal"`)
- Test: `features/channels/components/__tests__/StoryViewer.test.tsx`

**Interfaces:**
- Consumes: `useChannels`·`useChannelCards`·`useSeenChannels`(C1),
  `useSaveOptimistic(contentId)`(`@/features/saved/hooks/use-save-optimistic`),
  `getCurrentCoords`(`@/features/map/usecases/request-location`), `PermissionPrimer`
  (`@/features/map/components/PermissionPrimer`).
- Produces: 라우트 `/channels?start={key}` — C6 홈의 `onOpen`이
  `router.push(\`/channels?start=${key}\`)`로 진입. 이 라우트는 어디서도 링크되기
  전이므로 추가 자체는 안전.

- [ ] **Step 1: 실패 테스트 작성**

StoryViewer를 라우터 독립 컴포넌트로 두고(스크린 파일은 파라미터만 파싱) 테스트:

```ts
// mock: channels 메타 2개(hot→hidden), 각 cards 2장
test("renders progress segments equal to card count and English title", ...);
test("right tap advances card, left tap goes back", ...);
// v-taps testID right/left Pressable press → 표시 카드 이름 변경 확인
test("tap on last card of last channel closes viewer", ...);
// router.back 호출 mock 확인 + markSeen 호출
test("advancing past last card moves to next channel and marks seen", ...);
test("snap channel hides save and detail buttons", ...);
// saveable=false 카드 → "저장"/"자세히 보기" 부재
test("detail button closes viewer then pushes spot detail", ...);
// router.back + router.push(`/spots/${contentId}`)
```

- [ ] **Step 2: 실패 확인** — FAIL.

- [ ] **Step 3: 구현**

`StoryViewer`: 상태 `{channelIdx, cardIdx}`. 배경 `#141216`. 레이어 순서(프로토타입):
전체 배경 사진(`RemoteImage` absolute fill + 상하 SVG 그라데이션 스크림) → 좌 34%/우
나머지 투명 `Pressable` 탭존 → 상단(진행바 세그먼트 flex:1 h3 r2, done=흰색·current
55%·나머지 32% 흰색, 채널 라벨 16/800, 닫기 글래스 원형 32) → 하단
(`StoryCard`: 뱃지행[rank=글래스 원형 38 숫자 | dday=`colors.accent` pill | tag=글래스
pill] + 이름 26/800 + 지역 14 onDim + line 13.5/600 + 버튼행[저장 glass + 자세히 보기
solid white], `saveable=false`면 버튼행 생략). 탭 진행 로직은 프로토타입 스크립트
그대로: 우탭 = cardIdx+1, 끝이면 markSeen 후 다음 채널 0번(마지막 채널이면
`router.back()`), 좌탭 = cardIdx-1, 0이면 이전 채널 마지막 카드. 좌우 스와이프 채널
이동·아래 스와이프 닫기는 `PanResponder`(MapBottomSheet 패턴)로 수평/수직 제스처 판별
— 구현 부담 크면 이번 태스크는 탭·닫기만 배선하고 스와이프는 C7 전 폴리시 태스크로
분리하지 말고 여기서 완성한다(스펙 §4.3 조작 명세). 자동 진행(5초)은 스펙상 2순위 —
구현하지 않는다.
Around 채널 진입 시 coords 없으면: `getPermissionStatus()` → granted면
`getCurrentCoords()` 후 fetch, 아니면 `PermissionPrimer` 재사용 카드 1장(허용 버튼 →
`requestPermission()` → granted 시 fetch) — 스펙 §4.4.
`저장` 버튼: `useSaveOptimistic(card.contentId)` — 카드별 컴포넌트 분리로 훅 규칙 준수.
`src/app/channels.tsx`: `useLocalSearchParams<{start?: ChannelKey}>()` → `<StoryViewer start={...} />`.
`_layout.tsx`: `<Stack.Screen name="channels" options={{ presentation: "fullScreenModal", headerShown: false }} />`.

- [ ] **Step 4: 통과 + 게이트 + 커밋**

```bash
npx jest StoryViewer -v && npm run lint && npm run typecheck && npm test
git add src && git commit -m "feat(mobile): 채널 스토리 뷰어 (탭 진행·채널 연속·권한 카드)"
```

---

### Task C4: features/feed — 해외→국내 게시물 캐러셀

**Files:**
- Create: `features/feed/posts-api.ts`, `features/feed/posts-queries.ts`,
  `features/feed/components/PostCarousel.tsx`, `components/PostSlide.tsx`,
  `components/CreditSheet.tsx`
- Test: `features/feed/__tests__/posts-api.test.ts`,
  `features/feed/components/__tests__/PostCarousel.test.tsx`

(구 `api.ts`/`queries.ts`/`HeroCarousel`/`MoodRail`은 건드리지 않는다 — C6에서 삭제.)

**Interfaces:**
- Consumes: JSend 언랩 `api`, `useSaveOptimistic`, `prefetchSpot`(`@/features/spots/queries`)
  존재 시 매치 슬라이드 pressIn 워밍에 재사용.
- Produces:
  ```ts
  type OverseasPost = { id: number; nameKo: string; countryCode: string; countryNameKo: string;
    descriptionKo: string | null; imageUrl: string; imageAuthor: string | null;
    imageLicense: string | null; imageLicenseUrl: string | null; imageSourceUrl: string };
  type MatchCard = { contentId: string; title: string; regionLabel: string;
    imageUrl: string; overviewFirst: string | null };
  getPosts(params: { seed?: string; cursor?: string; limit?: number })
    : Promise<{ seed: string; items: OverseasPost[]; nextCursor: string | null; hasMore: boolean }>
  getMatches(id: number): Promise<{ overseasId: number; matches: MatchCard[] }>
  usePostsFeed(seedState) — useInfiniteQuery, queryKey ["posts", seed]
  useMatches(id, { enabled }) — queryKey ["matches", id], staleTime 6h
  <PostCarousel post={OverseasPost} />   // 홈 피드·탐색 모달 공용
  ```

- [ ] **Step 1: 실패 테스트 작성**

`posts-api.test.ts`: `/feed`·`/overseas/{id}/matches` 경로·파라미터 검증 (C1 패턴).
`PostCarousel.test.tsx`:

```ts
test("first slide shows overseas hook with country chip and info button", ...);
// nameKo·countryNameKo·descriptionKo 렌더, testID "credit-info" 존재, counter "1/4"
test("matches query stays disabled until first swipe", ...);
// useMatches mock — 초기 enabled=false 확인(모킹된 훅 인자 검사)
test("after swipe, match slides render number, name, region, overview", ...);
// useMatches가 matches 3개 반환하도록 mock + armed 상태 강제 → "1" 위첨자·이름·regionLabel·overviewFirst
test("match slide bookmark toggles via save hook", ...);
test("zero matches renders searching card", ...);
// matches [] → "비슷한 국내 여행지를 찾는 중이에요" 카드 1장, counter "2/2"
test("info button opens credit sheet with three rows", ...);
// 촬영/라이선스/제공 행 + imageAuthor 표시
```

- [ ] **Step 2: 실패 확인** — FAIL.

- [ ] **Step 3: 구현**

`PostCarousel`: 인셋 카드(`marginHorizontal:16`, radius 16, `shadows.card`, height 520,
overflow hidden) + 내부 `FlatList horizontal pagingEnabled getItemLayout`(PhotoViewer
패턴, snap 폭 = 카드 폭) + 카드 아래 중앙 dot 인디케이터(6pt, active `colors.ink`).
슬라이드 데이터: `[{kind:"hero", post}, ...matchSlides]` — matches 로딩 전에는
`{kind:"skeleton"}` 3장(총 4, counter n/4), 로드 후 실제 3장(미만이면 있는 만큼,
0이면 `{kind:"empty"}` 1장). 스와이프 시작(`onScrollBeginDrag`) 또는 인덱스≥1 진입
시 `setArmed(true)` → `useMatches(post.id, { enabled: armed })`.
`PostSlide`: hero — `RemoteImage` 풀블리드 + 상·하 SVG 그라데이션(프로토타입 스크림
스톱값) + 우상단 counter pill(글래스 다크) + 좌상단 ⓘ 글래스 원형 32(`<Icon name="info">`)
+ 하단 meta(이름 24/800 + 세로 구분선 나라 칩 13/600 onDim + 설명 14.5 2줄 clamp
`numberOfLines={2}`); match — 좌상단 북마크 글래스 원형 36
(`bookmark`/`bookmark-fill`, `useSaveOptimistic`) + 번호 위첨자(15/800 onDim,
`top:-7` 스타일) + 이름·지역칩 + `overviewFirst` 2줄 + 카드 전체 `Pressable` →
`router.push(\`/spots/${contentId}\`)`; skeleton — `colors.skeleton` 면 + 펄스 없이 정적;
empty — 중앙 문구 카드.
`CreditSheet`: AuthPromptSheet 구조 복제(RN Modal transparent slide + 그랩바). 타이틀
"사진 정보", 3행: 촬영=`imageAuthor ?? "-"` / 라이선스=`imageLicense` + ↗ →
`Linking.openURL(imageLicenseUrl)` / 제공="Wikimedia Commons" + ↗ →
`Linking.openURL(imageSourceUrl)`. 링크 행은 URL 없으면 비활성.
`posts-queries.ts`: `usePostsFeed` — `useInfiniteQuery({ queryKey: ["posts", seed],
queryFn: ({ pageParam }) => getPosts({ seed, cursor: pageParam ?? undefined, limit: 6 }),
getNextPageParam: (last) => (last.hasMore ? last.nextCursor : undefined),
initialPageParam: null })`. 첫 페이지 응답의 `seed`를 컴포넌트가 확정 저장(응답 seed ≠
요청 seed일 수 있는 건 최초 1회뿐).

- [ ] **Step 4: 통과 + 게이트 + 커밋**

```bash
npx jest src/features/feed -v && npm run lint && npm run typecheck && npm test
git add src && git commit -m "feat(mobile): 해외→국내 게시물 캐러셀 + 출처 시트"
```

---

### Task C5: features/explore — 시드 랜덤 그리드 + 게시물 모달

**Files:**
- Create: `features/explore/api.ts`, `features/explore/queries.ts`,
  `features/explore/lib/grid-blocks.ts`, `features/explore/components/ExploreGrid.tsx`,
  `features/explore/components/PostModal.tsx`
- Test: `features/explore/lib/__tests__/grid-blocks.test.ts`,
  `features/explore/components/__tests__/ExploreGrid.test.tsx`

**Interfaces:**
- Consumes: `PostCarousel`(C4 — feature 간 import는 ESLint 경계상 허용),
  `OverseasPost` 타입(C4 re-export).
- Produces: `<ExploreGrid />` (C6의 `(tabs)/explore.tsx`가 배치),
  `useExploreFeed(seedState)` — `["explore", seed]` useInfiniteQuery,
  `toGridBlocks(items: OverseasPost[]): GridBlock[]`.

- [ ] **Step 1: grid-blocks 실패 테스트**

```ts
type GridBlock =
  | { type: "row3"; items: [OverseasPost, OverseasPost, OverseasPost] }
  | { type: "big"; big: OverseasPost; side: [OverseasPost, OverseasPost] };

test("9 items become row3, big, row3", () => {
  const blocks = toGridBlocks(posts(9));
  expect(blocks.map((b) => b.type)).toEqual(["row3", "big", "row3"]);
});

test("partial tail under 3 items is dropped from blocks but kept in leftover", ...);
// toGridBlocks가 {blocks, leftover} 반환 — 다음 페이지 합류 시 재사용
test("big block consumes exactly 3 items", ...);
```

- [ ] **Step 2: 구현 — `toGridBlocks`**

9개 단위 사이클(row3 → big → row3), 항목 부족 시 완성 못한 블록은 leftover로 반환.
big 블록 렌더: 좌측 2×2 큰 타일 + 우측 세로로 작은 타일 2개(프로토타입 dense grid의
근사 — RN엔 CSS grid가 없으므로 이 레이아웃이 확정 구현).

- [ ] **Step 3: ExploreGrid 실패 테스트 → 구현**

테스트: 타일 탭 → PostModal 열림(내부에 PostCarousel), 새로고침 제스처 →
`refetch`+새 시드, 스크롤 끝 → `fetchNextPage` 호출.
구현: `FlatList`(data=blocks, renderItem이 블록 단위 View 행), 간격 2, 타일은
`RemoteImage` 정사각(`aspectRatio:1`) `Pressable`. **텍스트 없음.** 풀블리드:
화면 컴포넌트에서 SafeArea 상단 미적용 + 최상단 absolute 그라데이션 스크림(높이 92,
`rgba(20,18,22,.55)`→투명) + `<StatusBar style="light" />`(expo-status-bar).
`RefreshControl`로 당겨서 새로고침 → seed 상태 초기화(null) 후 refetch = 새 시드.
`PostModal`: RN Modal fullscreen(`animationType:"fade"`, 배경 `rgba(16,14,18,.92)`) +
중앙 `PostCarousel` + 우상단 닫기 글래스 버튼. 스크린 파일은 C6에서 붙인다.

- [ ] **Step 4: 통과 + 게이트 + 커밋**

```bash
npx jest src/features/explore -v && npm run lint && npm run typecheck && npm test
git add src && git commit -m "feat(mobile): 탐색 그리드(시드 랜덤·빅타일) + 게시물 모달"
```

---

### Task C6: 플립 PR — 탭바 교체 + 홈 교체 + 사진·큐레이션 제거 (원자적)

**Files:**
- Modify: `src/app/(tabs)/_layout.tsx`, `src/app/(tabs)/index.tsx`(전면 재작성),
  `src/app/_layout.tsx`(photo Stack.Screen 제거)
- Create: `src/app/(tabs)/explore.tsx`
- Delete: `src/app/(tabs)/photo.tsx`, `src/app/photo/`(4파일), `src/features/photo/`(전체),
  `src/app/curations/[slug].tsx`, `src/features/curation/`(전체),
  `src/features/feed/api.ts`, `features/feed/queries.ts`,
  `features/feed/components/HeroCarousel.tsx`, `components/MoodRail.tsx` + 각 테스트,
  `src/__tests__/curation-detail-screen.test.tsx`
- Modify: `src/lib/api-types.ts`(`PhotoMatch`·`PhotoSearchResult`·`HomeFeed`·`HeroTile`·
  `MoodRailDto`·`CurationDetail` 등 신 화면 미사용 타입 제거 — typecheck가 잔존 참조 검출),
  `src/__tests__/home-screen.test.tsx`(신 홈 기준 재작성)

**선행 게이트 (머지 전 필수):** A·B가 prod 배포되어 있고 아래가 실데이터로 응답:

```bash
curl -s "https://api.pictrip.org/v1/home/channels" | head -c 300
curl -s "https://api.pictrip.org/v1/feed?limit=2" | head -c 300
curl -s "https://api.pictrip.org/v1/explore?limit=3" | head -c 300
```

- [ ] **Step 1: 홈 스크린 테스트 재작성 (실패 상태로)**

`home-screen.test.tsx`: 기존 mock 대상을 `@/features/channels/queries`·
`@/features/feed/posts-queries`로 교체 — 워드마크 렌더, ChannelTiles 존재,
게시물 카드 렌더, 스크롤 끝 `fetchNextPage`, 새로고침 시 새 시드.

- [ ] **Step 2: 탭바·스크린 교체**

`(tabs)/_layout.tsx` — photo 인터셉트 리스너 삭제, 4탭:

```tsx
<Tabs.Screen name="index" options={{ title: "홈", tabBarIcon: tabIcon("home") }} />
<Tabs.Screen name="explore" options={{ title: "탐색", tabBarIcon: tabIcon("search") }} />
<Tabs.Screen name="map" options={{ title: "지도", tabBarIcon: tabIcon("map-pin") }} />
<Tabs.Screen name="profile" options={{ title: "마이", tabBarIcon: tabIcon("person") }} />
```

`(tabs)/index.tsx`: SafeArea + 기존 워드마크 탑바 유지 + `FlatList`
(header=`<ChannelTiles onOpen={(k) => router.push(\`/channels?start=${k}\`)} />`,
data=`usePostsFeed` 평탄화 posts, renderItem=`<PostCarousel post={item} />`,
`onEndReached={fetchNextPage}` threshold 0.8, `RefreshControl`=시드 리셋, footer=기존
legal 링크 푸터 이식). 로딩=타일·카드 스켈레톤, 에러=기존 홈 에러 뷰 관례.
`(tabs)/explore.tsx`: `<ExploreGrid />` 배치(+`StatusBar light` 처리).

- [ ] **Step 3: 삭제 목록 실행 + 타입 정리**

```bash
git rm -r src/app/photo src/app/(tabs)/photo.tsx src/features/photo \
  "src/app/curations" src/features/curation \
  src/features/feed/api.ts src/features/feed/queries.ts \
  src/features/feed/components/HeroCarousel.tsx src/features/feed/components/MoodRail.tsx
grep -rn "features/photo\|features/curation\|useHomeFeed\|HeroCarousel\|MoodRail\|photo-search" src
```
Expected: grep 0건 (남으면 그 참조까지 정리). `_layout.tsx`의
`<Stack.Screen name="photo" ...>` 제거, `curations/[slug]` 등록이 있으면 제거.
typed routes 재생성이 필요하면 `npx expo customize tsconfig.json` 없이
`npm run typecheck` 전 `.expo/types` 재생성 절차(README/기존 관례) 수행.

- [ ] **Step 4: 전체 게이트**

```bash
npm run lint && npm run typecheck && npm run format:check && npm test
```
Expected: 전부 통과. photo 관련 테스트가 스위트에서 사라지고 신 홈 테스트 통과.

- [ ] **Step 5: 시뮬레이터 스모크 (verify 스킬 절차)**

로컬 백엔드 대신 prod API로 확인(A·B가 이미 라이브):
Metro `--clear` 기동 → 홈: 타일 6종 렌더·탭→뷰어 진행/닫기·피드 스와이프 시 매칭
로드·ⓘ 시트 링크·저장 토글 → 탐색: 그리드·빅타일·탭→모달·새로고침 → 지도·마이 회귀
무손상 → 게스트 상태에서 저장 탭 시 로그인 게이트 동작. 시뮬 GPS는
`xcrun simctl location booted set 37.5665,126.9780`.

- [ ] **Step 6: 커밋 + PR (플립 단일 PR)**

```bash
git add -A && git commit -m "feat(mobile): S13 플립 — 4탭(홈·탐색·지도·마이)·신규 홈·사진 기능 제거"
```
PR 본문에 삭제 인벤토리·선행 게이트 curl 결과·스모크 확인 항목 명기. 머지 즉시 EAS
OTA로 전 사용자 JS 교체됨을 PR에 경고로 남긴다.

---

### Task C7: OTA 후 확인 + 기록

- [ ] **Step 1:** dev 머지 후 mobile-deploy 워크플로 성공 확인(`gh run list`), 실기기/
TestFlight 앱 재시작으로 신 홈 수신 확인.
- [ ] **Step 2:** 오리진 로그에서 `/v1/feed`·`/v1/home/channels` 트래픽 발생,
`/v1/home/feed`·`/v1/taste/photo-search` 트래픽 감소 추이 확인 시작 — Plan D의
제거 게이트 데이터.
- [ ] **Step 3:** 메모리/이슈에 남은 폴리시(스와이프 힌트 없음 확인, 자동 진행 5초 2순위
미구현) 기록.

---

## Self-Review 체크 결과

- 프로토타입 대비: 타일(86×110·seen 55%·그린 뱃지·Around 연그린)=C2, 뷰어(진행바·
  영어 타이틀·탭존 34%·채널 연속 진행·rank/dday/tag 뱃지·저장/자세히)=C3, 게시물
  (인셋 16·radius 16·520·counter pill·ⓘ·북마크·번호 위첨자·2줄 clamp·dot)=C4,
  탐색(3열 gap 2·i%7 빅타일 근사·상단 스크림 92·라이트 상태바·모달 재사용)=C5,
  탭바 4종 아이콘=C6. 출처 시트 3행·링크=C4.
- 스펙 §5.1 "다음 게시물 카드 상단 살짝 노출"은 FlatList 세로 스크롤 특성상 자연 충족
  (고정 peek 아님 — 프로토타입도 스크롤 연속). §9 제거 목록 전부 C6 Step 3에 포함.
- 아이콘: 필요 글리프(home·search·map-pin·person·bookmark·info·close) 전부 기존
  IconName에 존재 — 신규 아이콘 작업 불필요.
