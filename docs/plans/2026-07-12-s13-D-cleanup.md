# S13 Plan D — 레거시 제거 (photo-search · /home/feed · 큐레이션 표면 · 문서)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** C 플립이 라이브·검증된 뒤 구 표면을 제거해 코드베이스를 신 구조로 수렴시킨다.

**Architecture:** 제거 전용. DB 테이블(`curations`·`curation_spots`,
`users.taste_vector` 컬럼)은 이번에 드롭하지 않는다 — 파괴적 변경은 별도 판단
(코드가 더 이상 참조하지 않는 상태로 만들어 두는 것까지가 이 계획).

**Global Constraints:** 개요 문서 §Global Constraints. 브랜치 `chore/s13-d-cleanup`.
**선행: Plan C Task C6 머지 + OTA 유예기간 경과.**

---

### Task D0: 제거 게이트 — 구 엔드포인트 트래픽 확인

- [ ] **Step 1: 오리진 로그에서 잔존 호출 확인**

```bash
ssh root@100.83.101.1 "pct exec 112 -- docker logs api-host-api-1 --since 48h 2>&1 \
  | grep -cE 'POST /v1/taste/photo-search|GET /v1/home/feed|GET /v1/curations/'"
```
Expected: 0 또는 무시 가능한 수준(구버전 바이너리 잔존 사용자). 유의미한 트래픽이
남아 있으면 **여기서 중단**하고 며칠 뒤 재확인 — OTA 미수신 바이너리 사용자의 홈이
바로 깨지기 때문. 판단 근거(카운트·기간)를 PR 본문에 기록한다.

---

### Task D1: 백엔드 — taste 모듈·`/home/feed` 제거

**Files:**
- Delete: `backend/app/modules/taste/` 전체, `backend/tests/test_taste_photo_search.py`
- Modify: `backend/app/main.py` (taste import·include_router 제거),
  `backend/pyproject.toml` (import-linter 4개 계약에서 taste 경로 제거),
  `backend/alembic/env.py` (taste models import 제거),
  `backend/app/config.py` (`PHOTO_SEARCH_SIMILARITY_FLOOR`·`PHOTO_SEARCH_MAX` 제거),
  `backend/app/modules/images/repositories.py`
  (`find_neighbor_ids_by_vector_direct` — taste 유일 소비였음 → 삭제, 단 grep로 재확인),
  `backend/app/modules/spots/routes.py` (`GET /home/feed` 제거),
  `backend/app/modules/spots/services/feed.py` 삭제 + `services/__init__.py` 재수출 정리,
  `backend/app/modules/spots/schemas.py` (`HomeHero`·`HomeRail`·`HomeFeedResponse` 제거),
  `backend/tests/test_home_feed.py` 삭제

- [ ] **Step 1: 삭제 실행 + 참조 스윕**

```bash
cd backend && git rm -r app/modules/taste tests/test_taste_photo_search.py
git rm app/modules/spots/services/feed.py tests/test_home_feed.py
grep -rn "taste\|photo_search\|PHOTO_SEARCH\|assemble_home_feed\|HomeFeedResponse\|HomeHero\|HomeRail\|find_neighbor_ids_by_vector_direct" app tests scripts pyproject.toml alembic
```
grep 결과가 가리키는 모든 잔존 참조를 정리. 주의: `users.taste_vector` **컬럼과
0005 마이그레이션·users/models.py 선언은 유지** — 계정 삭제 시 reset 코드
(`users/services.py:157`)만 컬럼과 함께 남겨둔다(드롭은 후속 마이그레이션 판단).

- [ ] **Step 2: 전체 게이트**

```bash
uv run ruff check . && uv run ruff format --check . && uv run mypy app && uv run lint-imports
POSTGRES_DB=pictrip_test NO_COLOR=1 uv run pytest
```
Expected: 전부 통과 (207개 대비 taste·home_feed 테스트 수만큼 감소).

- [ ] **Step 3: 커밋**

```bash
git add -A && git commit -m "chore(backend): taste 모듈·/home/feed 제거 — S13 신 피드로 대체 완료"
```

---

### Task D2: 큐레이션 표면 은퇴 (백엔드 라우트 + 어드민 편집기)

**Files:**
- Modify: `backend/app/modules/spots/routes.py` (`GET /curations/{slug}` 제거),
  `backend/app/modules/spots/services/curations.py`
  (어드민 프리뷰가 쓰는 `resolve_curation_spots` 계열만 남기고, 어드민 편집기까지
  제거하므로 사실상 전체 삭제 — 아래 Step 1 grep이 SSOT),
  `backend/app/modules/admin/routes.py`·`services.py`·`repositories.py`·`schemas.py`
  (curation HTML 페이지 + `/api/curations*` 8개 + `/api/spots/search` 중 큐레이션
  피커 전용이면 함께 제거), `backend/scripts/seed_curations.py` 삭제
- Delete: `admin/mockups/curation.html`, `admin/mockups/assets/curation.{js,css}` +
  `backend/app/modules/admin/static/` 동기 삭제, 나머지 페이지 네비에서 "큐레이션" 링크 제거
- Delete: 관련 테스트 (`tests/test_admin_curations*.py`, `tests/test_curation*.py` 등 실측)
- Modify: `web/` 딥링크 `/curations/*` 폴백 페이지가 있으면 안내 문구 유지 여부 확인
  (딥링크 OG Function이 큐레이션 slug를 참조하면 스팟 전용으로 축소)

- [ ] **Step 1: 소비자 grep으로 삭제 경계 확정**

```bash
grep -rn "curation" backend/app backend/tests backend/scripts web --include="*.py" --include="*.js" --include="*.html" -il
```
원칙: **모델(`Curation`·`CurationSpot`)과 마이그레이션 0011은 남긴다.** 그 외
서빙·편집·시드 코드는 전부 제거. `alembic/env.py`는 spots.models를 이미 import하므로
모델 잔존으로 autogenerate 드리프트 없음.

- [ ] **Step 2: 삭제 실행 + drift check + 게이트**

```bash
rsync -a --delete --exclude README.md admin/mockups/ backend/app/modules/admin/static/
bash .github/scripts/check-admin-mockup-drift.sh
cd backend && uv run ruff check . && uv run mypy app && uv run lint-imports && \
POSTGRES_DB=pictrip_test NO_COLOR=1 uv run pytest
```

- [ ] **Step 3: 커밋**

```bash
git add -A && git commit -m "chore: 큐레이션 표면 은퇴 (라우트·어드민 편집기·시드) — 테이블은 보존"
```

---

### Task D3: 문서·설정 수렴

**Files:**
- Modify: `CLAUDE.md`, `docs/specs/README.md`(있다면 S02·S04 상태 표기),
  `docs/specs/S02-home-curation.md`·`S04-photo-search.md` 상단에 "S13으로 대체됨" 배너,
  `docs/specs/S09-api-contract.md` (신 엔드포인트 반영·제거분 삭제)

- [ ] **Step 1: CLAUDE.md 갱신**

- Stack 절: 모듈 나열 `users · taste · spots · images · map · system · admin` →
  `users · spots · images · map · feed · system · admin`.
- Backend DB facts: 큐레이션·홈 피드 항목을 신 구조로 교체 —
  `overseas_spots`(백엔드 소유, 파이프라인 적재)·`match:{overseasId}` Redis 1줄,
  `spot_concentration` 크론 1줄, "Home feed는 `/feed`(해외→국내 게시물)" 1줄.
- Repo layout·Prohibitions는 변경 없음(이미 KTO 규칙 일반형). Commons 표기 의무 1줄 추가.

- [ ] **Step 2: 스펙 배너 + API 계약 갱신 + 커밋**

```bash
git add CLAUDE.md docs && git commit -m "docs: S13 반영 — 모듈 구성·API 계약·구 스펙 배너"
```

- [ ] **Step 3: 배포 확인**

dev 머지 → CT112 자동 배포 후:

```bash
curl -s -o /dev/null -w "%{http_code}" https://api.pictrip.org/v1/home/feed        # 404
curl -s -o /dev/null -w "%{http_code}" -X POST https://api.pictrip.org/v1/taste/photo-search  # 404 or 405
curl -s "https://api.pictrip.org/v1/feed?limit=1" | head -c 200                    # 200 + data
```

---

## Self-Review 체크 결과

- 스펙 §9 제거 목록 완전 소화: 모바일분은 C6, 백엔드 `taste`(모듈 통째 — taste_vector
  의존이 없음은 조사로 확인됨)·`/home/feed` 조립은 D1, 어드민 큐레이션 정리(스펙 §8
  "구현 계획에서 범위 확정")는 D2에서 "라우트·UI 제거 + 테이블 보존"으로 확정.
- 유지 목록 준수: CLIP 파이프라인·`spot_embeddings`·hnsw 설정은 어떤 태스크도
  건드리지 않는다.
- D0 게이트가 구버전 사용자 보호 장치 — 통과 전 D1 진행 금지.
