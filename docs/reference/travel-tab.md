# 여행 탭 화면 명세

> 여행 탭(구 플랜)의 구성 요소·수치·상태 전이 조회표. 전환 배경은
> [ADR 0009](../adr/0009-travel-tab-conversational-agent.md), 색·간격 정본은
> `mobile/src/constants/theme.ts`.

탭은 홈 · 탐색 · **여행** · 마이 4개. 여행 탭만 이 문서의 범위다.

## 화면 구조

`src/app/(tabs)/travel.tsx` 한 화면 안에 3층이 쌓인다.

| 층 | 내용 | 비고 |
|---|---|---|
| 상단 바 | 워드마크 `PICTRIP` + 초록 점 | 실앱 그대로, 하단 1px `line` |
| 스크롤 | 헤드라인 → 채널 3단 → 대화 | 대화가 아래로 이어붙는 단일 스크롤 |
| 도크 | 조건 칩 + 제안 칩 + 컴포저 | 탭바 위 1층 고정, 배경 `bg` + 상단 1px `line` |

헤드라인은 `오늘,\n어디로 갈까요` 한 줄뿐 — 서브카피·갱신 스탬프·출처 각주 없음.
25/800, letterSpacing -0.8, lineHeight 1.34, padding 24/20/6.

## 채널 3단

| 섹션 | 소스 | 카드 태그 | 위치 권한 |
|---|---|---|---|
| 인기 관광지 | `GET /v1/home/channels/hot` | `붐빔` | 불필요 |
| 숨은 관광지 | `GET /v1/home/channels/hidden` | `하위 N%` | 불필요 |
| 내 근처 | `GET /v1/home/channels/around?lat=&lng=` | `N.Nkm` | **필요** |

- `hot` · `hidden`은 `spot_concentration` 일일 크론(04:30 KST) 소스.
- 위치 권한이 없으면 `내 근처` 섹션만 "위치를 켜면 근처를 찾아드려요" 한 행으로
  대체하고, 나머지 두 섹션은 그대로 그린다. 섹션 자체를 숨기지 않는다.
- 섹션 헤더 = 제목 18/800 + 개수 14/800 `accentText` + 우측 `전체 ›` 12.5/700 `ter`.
- 레일에는 5장, `전체`는 리스트 화면으로 전체를 넘긴다.

### 스팟 카드

사진 위에 캡션을 얹는 홈 `PostSlide` 문법. 값은 실측 기준이며 실기기에서
1.9장 걸침이 "더 있다" 신호로 약하면 170px(2장 딱 떨어짐)로 조정한다.

| 요소 | 값 |
|---|---|
| 크기 | 레일 158×206, 리스트 그리드 48%×172, radius 16, gap 11 |
| 이미지 | `cover`, 배경 `skeleton`, 로드 실패 시 opacity 0 |
| scrim | `linear-gradient(180deg, rgba(20,18,22,.16) 0%, transparent 32%, transparent 46%, rgba(16,14,18,.80) 100%)` + inset 1px `line` |
| 상태 태그 | 좌상단 9/9, h24, radius 999, 배경 `control` + blur 8, 11/800 `onImage` |
| 하트 | 우상단 9/9, 28×28, 배경 `control` + blur 8, 활성 시 `danger` + fill |
| 캡션 | 좌우 12 / 하단 12, 제목 14.5/800 `onImage` 1줄, 지역 11.5 `onDim` 1줄 |
| 탭 | 카드 → 스팟 상세, 하트 → 저장 토글 + 토스트 (`scale(.975)` 프레스) |

이미지 URL은 백엔드가 서명한 `img.pictrip.org` 프록시 URL을 **그대로** 통과시킨다
(`cpyrhtDivCd=Type3`는 무변형 pass-through — [ADR 0005](../adr/0005-kto-image-policy.md)).

## 대화

채널 3단 아래, 상단 1px `line` + padding-top 22로 구분되는 영역. 턴이 비면
영역 자체가 없다. 각 턴은 아래 순서로 쌓인다.

| 순서 | 요소 | 사양 |
|---|---|---|
| 1 | 사용자 버블 | 우측 정렬, 배경 `ink` / `onImage`, radius 14, padding 11/14, 최대 폭 80%. 사진 첨부 시 버블 위에 104px 썸네일 |
| 2 | 조회 단계 카드 | 1px `line` 박스 radius 16 padding 16. 행마다 스피너 → 체크(`accentText`), 우측에 개수 배지 11/700 `accentText` on `accentFill` |
| 3 | 답변 문장 | 14.5/1.55, 숫자·핵심어는 `<b>` = `accentText` 800 |
| 4 | 제안 칩 | h34 radius 999, 1px `line`, 13/700 `sec` — 탭 시 즉시 재질의 |
| 5 | 결과 레일 | 스팟 카드 4장 가로 레일 (padding 0) |
| 6 | 푸터 | 좌 `전체 N곳 보기 ›`, 우 피드백 버튼(토글 시 `fill` 배경) |

- 턴 등장 = `translateY(9px)` + opacity, 0.32s `cubic-bezier(.2,.7,.3,1)`.
- 단계 재생은 **클라이언트 애니메이션**이다. 서버가 실행한 단계를 순서대로
  받아 0.76초 간격으로 재생한다(3단계 = 약 2.3초). 응답이 이보다 빨리 와도
  최소 표시 시간을 지키고, 늦게 오면 마지막 단계를 스피너로 붙잡는다.
- 재생 중에는 새 질의를 받지 않는다(도크 전송 비활성).
- 답변이 끝나면 제안 칩이 도크 칩으로도 승격된다.

## 도크

| 요소 | 사양 |
|---|---|
| 조건 칩 | 항상 첫 칩. 기본 라벨 `조건 ›`, 적용되면 `수도권 · 이번 주말`로 바뀌고 `ink` 배경으로 활성 |
| 제안 칩 | 초기 4개(`여름에 시원하고 사람 적은 계곡` 등) → 답변 후 그 턴의 제안으로 교체 |
| 칩 공통 | h34, padding 0/16, radius 999, 1px `line`, 13.5/700 `sec`, gap 8, 가로 스크롤 |
| 컴포저 | 배경 `inset` + 1px `line`, radius 999, padding 5, margin 4/20/14 |
| `+` 버튼 | 36×36, `sec` — 사진 첨부 |
| 입력 | 14.5/500, placeholder `무엇이든 물어보세요` (첨부 시 `사진에 덧붙일 말 (선택)`) |
| 전송 | 36×36, 기본 `control`, 입력 또는 첨부가 있으면 `ink` |
| 첨부 배너 | 컴포저 위, `accentFill` + 1px `rgba(3,199,90,.28)`, 38px 썸네일 + "사진 1장 첨부됨 / 서버에 저장하지 않고 비교 후 폐기해요" |

첨부만 있고 텍스트가 없으면 질문은 `이 사진 같은 분위기의 여행지`로 보낸다.

## 조건 시트

자유문이 아니라 바텀시트로만 받는 정형 조건. `dim`(`scrim`) 탭 또는 적용으로 닫힌다.
시트 radius 20/20/0/0, padding 10/24/40, 전이 0.32s `cubic-bezier(.2,.8,.2,1)`.

| 그룹 | 값 | 기본 |
|---|---|---|
| 지역 | 전국 · 수도권 · 강원 · 충청 · 전라 · 경상 · 제주 | 전국 |
| 언제 | 상관없음 · 오늘 · 이번 주말 · 다음 주 | 상관없음 |
| 누구와 | 상관없음 · 혼자 · 둘이 · 아이와 · 반려견과 | 상관없음 |

- 각 그룹 단일 선택. 세그먼트 h34 radius 999, 선택 시 `ink` 배경.
- 적용 버튼 h52 radius 12 `ink`.
- 칩 라벨은 기본값(`전국` · `상관없음`)을 뺀 나머지를 ` · `로 잇는다. 남는 게
  없으면 `조건`.
- 조건은 이후 모든 질의에 함께 실린다. LLM을 거치지 않고 그대로 필터가 된다.

## 리스트 화면

`app/travel/results.tsx`. 결과는 서버에 저장되지 않으므로 직전 턴의 스팟 목록을
스토어에서 읽는다. 우측에서 슬라이드 인 (`translateX(100%)` → 0, 0.32s).

| 요소 | 사양 |
|---|---|
| 내비 | 뒤로 36×36 + 제목 16/800 + 부제 `N곳` 12 `ter`, 하단 1px `line` |
| 그리드 | 2열, `space-between`, rowGap 16, 카드 48%×172 |
| 본문 | padding 16/20/40 |

진입점은 채널 섹션의 `전체`와 대화 푸터의 `전체 N곳 보기` 두 곳. 카드 태그는
진입점의 태그 함수를 그대로 물려받는다(집중률 · 거리 · 유사도).

## 상태

| 상황 | 처리 |
|---|---|
| 위치 권한 없음 | `내 근처` 섹션만 안내 행으로 대체 |
| 채널 조회 실패 | 해당 섹션만 숨김, 나머지 섹션과 대화는 정상 |
| 질의 실패 | 해당 턴의 단계 카드를 에러 문구로 교체, 재시도 칩 노출 |
| 사진 첨부 실패 | 토스트 "사진을 불러오지 못했어요. 사진 접근 권한을 확인해 주세요." |
| 재생 중 | 도크 전송 비활성, 중복 질의 무시 |

토스트는 `PlanToast` 문법 — 탭바 위 104, 배경 `ink`, radius 12, 2.6초.

---

관련: [ADR 0009](../adr/0009-travel-tab-conversational-agent.md) ·
[api](api.md) · [architecture](../explanation/architecture.md)
