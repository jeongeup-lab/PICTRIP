name: backend-ci
on:
  pull_request:
    paths: ["backend/**", ".github/workflows/backend-ci.yml"]
# Successive pushes to the same PR cancel the in-flight run — only the latest
# commit's result matters here (deploy workflows keep cancel-in-progress: false).
concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: true
defaults:
  run:
    working-directory: backend
jobs:
  check:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        # User/DB/password must match app config defaults (app/config.py) so the
        # default connection URL resolves; pytest only overrides POSTGRES_DB.
        env:
          POSTGRES_DB: pictrip_test
          POSTGRES_USER: pictrip
          POSTGRES_PASSWORD: pictrip_dev_only
        ports: ["5432:5432"]
        options: >-
          --health-cmd pg_isready --health-interval 10s
          --health-timeout 5s --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy app
      - run: uv run alembic upgrade head
        env:
          POSTGRES_DB: pictrip_test
      - run: uv run pytest
        env:
          POSTGRES_DB: pictrip_test
